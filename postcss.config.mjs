/**
 * PostCSS configuration for Tailwind CSS v4 pipeline.
 * @type {import('postcss-load-config').Config}
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
